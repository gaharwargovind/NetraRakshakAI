"""JPEG compression artifact generators."""
import numpy as np


def apply_compression_artifact(image: np.ndarray, quality: int) -> np.ndarray:
    """Apply JPEG compression loss."""
    raise NotImplementedError("Compression generation is scheduled for Phase 11.")